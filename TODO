 - Maybe[...]
 - wrap :Native subs with predefined subs such as sub :Native('some.lib') { warn 'what about this code here?' }
 - expose offsetof( $struct, $id );
 - packed structs and unions by value
 - when long doubles are used on old (pre-5.36 perls) sizes are... odd
 - flesh out CodeRef[] for passing aggr types to CV*